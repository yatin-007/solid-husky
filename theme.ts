import { createTheme } from '@suid/material/styles';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#1B98F5', // Blue
      dark: 'red', // Light Orange 2
      light: '#FFFCF1', // Light Orange 3
      contrastText: '#FFFFFF',
    },
  },
  // Add more theme properties as needed
});
